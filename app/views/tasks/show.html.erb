
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "All the tasks", tasks_path %>

<%= link_to "Edit task", task_edit_path %>

<%= link_to "DELETE", task_delete_path, method: :delete, data: {confirm: "are you sure?"} %>
